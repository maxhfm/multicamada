# ── Service (ClusterIP — interno) ─────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: app-multicamada-backend-service
spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
