# ── Secret — credenciais do banco ─────────────────────────────────────────────
# Os valores abaixo são exemplos em base64.
# Gere com:  echo -n 'seu_valor' | base64
apiVersion: v1
kind: Secret
metadata:
  name: app-multicamada-mysql
type: Opaque
data:
  root-password: cm9vdHBhc3M=       # rootpass
  db-password:   Y2hhbmdlbWU=       # changeme
