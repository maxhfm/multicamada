# ── Service (Clu sterIP — interno) ─────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: multilayer-app
spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080