# ── Service (Clu sterIP — interno) ─────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: max-martins-dev
spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080