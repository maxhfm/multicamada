# ── Secret — credenciais do banco ─────────────────────────────────────────────
# Os valore s abaixo são exemplos em base64.
# Gere com:  echo -n  'seu_valor' | base64
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: max-martins-dev
data:
  DB_HOST: "mysql-service"
  DB_NAME: "appdb"
  DB_USER: "appuser"